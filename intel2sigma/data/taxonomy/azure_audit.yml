# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=auditlogs
# Vetted-stratum rule count at last calibration: 38
# Hand-review the field selections + modifiers before ship.

id: azure_audit
label: An Azure AD audit event was logged
description: Use for Azure AD audit log rules — directory changes, app registrations, MFA changes.
category_group: audit_and_identity
logsource:
  product: azure
  service: auditlogs
platforms:
  - id: azure
    product: azure
    tier: primary
synonyms: []
fields:
  - name: properties.message
    label: properties.message
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: Category
    label: Category
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: OperationName
    label: OperationName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: Status
    label: Status
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: LoggedByService
    label: LoggedByService
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: ActivityDisplayName
    label: ActivityDisplayName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
