# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=auditlogs
# Vetted-stratum rule count at last calibration: 38
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.

id: azure_audit
label: An Azure AD / Entra ID audit event was logged
description: Use for Azure AD / Entra ID audit log rules — directory changes, application registrations, role assignments, conditional access policy changes, B2B invites, MFA configuration.
category_group: audit_and_identity
logsource:
  product: azure
  service: auditlogs
platforms:
- id: azure
  product: azure
  tier: primary
synonyms:
- Azure AD audit
- Entra ID audit
- AAD audit log
- directory audit
- app registration
- B2B invite
- conditional access change
fields:
- name: properties.message
  label: Activity description
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Invite external user
  note: "Human-readable activity description — most-used field in this category. Common values: 'Invite external user', 'Add member from group', 'Update role setting in PIM', 'Add app role assignment to user'."
- name: Category
  label: Audit category
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: UserManagement
  note: "Top-level audit category. Common: UserManagement (user lifecycle), RoleManagement (RBAC), Policy (conditional access / authentication methods), ApplicationManagement, GroupManagement."
- name: OperationName
  label: Operation name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Authentication Methods Policy Update
  note: "Specific operation performed. More granular than properties.message — useful when you need to filter to a precise admin action (e.g. 'Update User Risk and MFA Registration Policy')."
- name: Status
  label: Operation status
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: LoggedByService
  label: Logging service
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: ActivityDisplayName
  label: Activity display name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
