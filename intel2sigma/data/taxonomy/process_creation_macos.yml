# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: macos/process_creation
# Vetted-stratum rule count at last calibration: 67
# Hand-review the field selections + modifiers before ship.

id: process_creation_macos
label: A macOS process was started
description: Use when malware or suspicious activity created a new process on macOS (ESF/EndpointSecurity events).
category_group: process_and_execution
logsource:
  product: macos
  category: process_creation
platforms:
  - id: macos
    product: macos
    tier: primary
synonyms: []
fields:
  - name: Image
    label: Image
    type: path
    default_modifier: endswith
    allowed_modifiers:
      - endswith
      - contains
      - startswith
      - exact
      - re
  - name: CommandLine
    label: CommandLine
    type: string
    default_modifier: contains
    allowed_modifiers:
      - contains
      - startswith
      - endswith
      - exact
      - re
  - name: ParentImage
    label: ParentImage
    type: path
    default_modifier: endswith
    allowed_modifiers:
      - endswith
      - contains
      - startswith
      - exact
      - re
  - name: OriginalFileName
    label: OriginalFileName
    type: string
    default_modifier: contains
    allowed_modifiers:
      - contains
      - startswith
      - endswith
      - exact
      - re
