# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: bitbucket/service=audit
# Vetted-stratum rule count at last calibration: 14
# Hand-review the field selections + modifiers before ship.

id: bitbucket_audit
label: A Bitbucket audit event was logged
description: Use for Bitbucket Data Center / Cloud audit rules — permission changes, repository and branch operations, token creation and authentication events in the source-control platform.
category_group: audit_and_identity
logsource:
  product: bitbucket
  service: audit
platforms:
- id: bitbucket
  product: bitbucket
  tier: primary
synonyms:
- Bitbucket audit
- Bitbucket Data Center
- repository audit
- SCM audit
- source control audit
fields:
- name: auditType.action
  label: Audit action name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: The specific audited operation, in Bitbucket's dotted action vocabulary.
  example: Global permission changed
- name: auditType.category
  label: Audit category
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Broad grouping the action belongs to — permissions, repositories, authentication.
  example: Permissions
