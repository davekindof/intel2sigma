# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: m365/service=threat_management
# Vetted-stratum rule count at last calibration: 13
# Hand-review the field selections + modifiers before ship.

id: m365_threat_management
label: A Microsoft 365 threat-management event was logged
description: Use for Microsoft 365 Defender threat-management rules — phishing and malware verdicts, Safe Links and Safe Attachments detections reported through the unified audit log.
category_group: audit_and_identity
logsource:
  product: m365
  service: threat_management
platforms:
- id: m365
  product: m365
  tier: primary
synonyms:
- M365 threat management
- Defender for Office
- Safe Links
- Safe Attachments
- phishing verdict
fields:
- name: eventSource
  label: Event source
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Which Microsoft 365 component reported the event.
  example: SecurityComplianceCenter
- name: eventName
  label: Event name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: The threat-management event recorded, naming the detection or policy action.
  example: TIMailData-Inline
- name: status
  label: Detection status
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Outcome the service assigned — delivered, blocked, replaced.
  example: Blocked
- name: Payload
  label: Raw event payload
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  note: Full JSON payload of the alert. Broad, so substring matching is usually the practical approach.
