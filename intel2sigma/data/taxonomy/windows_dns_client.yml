# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=dns-client
# Vetted-stratum rule count at last calibration: 6
# Hand-review the field selections + modifiers before ship.

id: windows_dns_client
label: A DNS query was logged by the Windows DNS client
description: Use for Windows DNS Client rules — endpoint-side name resolution, which catches C2 and exfiltration domains at the host rather than at the network.
category_group: network
logsource:
  product: windows
  service: dns-client
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- DNS client
- dns-client
- endpoint DNS
- name resolution
- DNS query
fields:
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  note: Microsoft-Windows-DNS-Client/Operational event. 3008 is the completed-query record.
  example: '3008'
- name: QueryName
  label: Queried domain name
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  note: The domain being resolved. Match on suffix for a whole domain, or on substring for a family of hosts.
  example: anonfiles.com
