# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/process_access
# Vetted-stratum rule count at last calibration: 23
# Hand-reviewed for verbiage 2026-04-26 (F1d).

id: process_access
label: A process opened a handle to another process
description: Use for Sysmon Event ID 10 (ProcessAccess) — credential dumping, code injection, debugger access, and other inter-process behaviors.
category_group: process_and_execution
logsource:
  product: windows
  category: process_access
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- ProcessAccess
- Sysmon EID 10
- credential dumping
- LSASS access
- process handle opened
- code injection
fields:
- name: SourceImage
  label: Source process path
  type: path
  default_modifier: endswith
  allowed_modifiers:
  - endswith
  - contains
  - startswith
  - exact
  - re
  example: '\rundll32.exe'
  note: "The process that opened the handle. Filter to processes that legitimately access others (e.g. taskmgr.exe) to find unusual openers."
- name: TargetImage
  label: Target process path
  type: path
  default_modifier: endswith
  allowed_modifiers:
  - endswith
  - contains
  - startswith
  - exact
  - re
  example: '\lsass.exe'
  note: "The process whose handle was opened. lsass.exe is the canonical credential-dumping target; csrss.exe, winlogon.exe, and SYSTEM-protected processes are also high-signal."
- name: GrantedAccess
  label: Granted access mask
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: '0x1410'
  note: "Hex bitmask of access rights. 0x1410 / 0x1438 = read VM + query info (mimikatz pattern). 0x40 / 0x1F0FFF = full access (debug-mode injection)."
- name: CallTrace
  label: Call trace
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: Provider_Name
  label: Provider name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: SourceUser
  label: Source user account
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
