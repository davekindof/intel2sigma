# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/registry_event
# Vetted-stratum rule count at last calibration: 32
# Hand-review the field selections + modifiers before ship.

id: registry_event
label: A registry value, key, or stream was created or deleted
description: Use for Sysmon EID 12 (RegistryEvent — create/delete). Distinct from registry_set (EID 13).
category_group: file_and_registry
logsource:
  product: windows
  category: registry_event
platforms:
- id: windows
  product: windows
  tier: primary
synonyms: []
fields:
- name: TargetObject
  label: Target object (path or registry key)
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: Details
  label: Value details
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: Image
  label: Executable path
  type: path
  default_modifier: endswith
  allowed_modifiers:
  - endswith
  - contains
  - startswith
  - exact
  - re
- name: EventType
  label: Registry operation type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: NewName
  label: New name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
