# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: gcp/service=gcp.audit
# Vetted-stratum rule count at last calibration: 16
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.
#
# GCP audit logs exist in two field-naming conventions in the Sigma
# corpus depending on the pipeline: the ECS-mapped flat ``gcp.audit.*``
# form (used by most SigmaHQ rules) and the raw nested
# ``data.protoPayload.*`` form. We list both so rules in either form
# load cleanly; in practice top-3 fields are all gcp.audit.*.

id: gcp_audit
label: A Google Cloud audit event was logged
description: Use for GCP Cloud Audit Log rules — IAM changes, GCE / GKE / GCS / BigQuery operations, Kubernetes RBAC, billing-account changes, and any GCP service that emits audit logs.
category_group: audit_and_identity
logsource:
  product: gcp
  service: gcp.audit
platforms:
- id: gcp
  product: gcp
  tier: primary
synonyms:
- GCP audit log
- Google Cloud audit
- Cloud Audit Logs
- GKE audit
- IAM change
- BigQuery audit
fields:
- name: gcp.audit.method_name
  label: Method name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: io.k8s.authorization.rbac.v*.clusterrolebindings.create
  note: "GCP API method invoked, in dotted lowercase form. For Kubernetes / GKE rules use the `io.k8s.<api-group>.<version>.<resource>.<verb>` shape (often with `v*` to match all API versions)."
- name: data.protoPayload.methodName
  label: Method name (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: "Same value as gcp.audit.method_name but in the raw nested protoPayload form. Use this only if your pipeline preserves the original GCP audit log structure rather than the ECS-flattened form."
- name: gcp.audit.service_name
  label: GCP service name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: container.googleapis.com
  note: "The GCP service that emitted the event (e.g. `container.googleapis.com` for GKE, `iam.googleapis.com`, `storage.googleapis.com`, `bigquery.googleapis.com`)."
- name: data.protoPayload.serviceName
  label: GCP service name (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.authorizationInfo.granted
  label: Authorization granted (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.authorizationInfo.permission
  label: IAM permission requested (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.logName
  label: Log name (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.resource.type
  label: Resource type (raw payload)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
