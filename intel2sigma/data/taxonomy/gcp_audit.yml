# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: gcp/service=gcp.audit
# Vetted-stratum rule count at last calibration: 16
# Hand-review the field selections + modifiers before ship.

id: gcp_audit
label: A Google Cloud audit event was logged
description: Use for GCP Audit Log rules — IAM, GCE, GKE, GCS.
category_group: audit_and_identity
logsource:
  product: gcp
  service: gcp.audit
platforms:
- id: gcp
  product: gcp
  tier: primary
synonyms: []
fields:
- name: gcp.audit.method_name
  label: Gcp.audit.method name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.authorizationInfo.granted
  label: Data.proto payload.authorization info.granted
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.authorizationInfo.permission
  label: Data.proto payload.authorization info.permission
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.logName
  label: Data.proto payload.log name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.methodName
  label: Data.proto payload.method name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.resource.type
  label: Data.proto payload.resource.type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: data.protoPayload.serviceName
  label: Data.proto payload.service name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
