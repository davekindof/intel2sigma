# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: gcp/service=gcp.audit
# Vetted-stratum rule count at last calibration: 16
# Hand-review the field selections + modifiers before ship.

id: gcp_audit
label: A Google Cloud audit event was logged
description: Use for GCP Audit Log rules — IAM, GCE, GKE, GCS.
category_group: audit_and_identity
logsource:
  product: gcp
  service: gcp.audit
platforms:
  - id: gcp
    product: gcp
    tier: primary
synonyms: []
fields:
  - name: gcp.audit.method_name
    label: gcp.audit.method_name
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.authorizationInfo.granted
    label: data.protoPayload.authorizationInfo.granted
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.authorizationInfo.permission
    label: data.protoPayload.authorizationInfo.permission
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.logName
    label: data.protoPayload.logName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.methodName
    label: data.protoPayload.methodName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.resource.type
    label: data.protoPayload.resource.type
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: data.protoPayload.serviceName
    label: data.protoPayload.serviceName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
