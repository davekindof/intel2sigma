# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: unspecified/proxy
# Vetted-stratum rule count at last calibration: 29 (55 across all strata)
# Hand-reviewed for verbiage 2026-04-26 (F1d-γ).
# Examples mined from real corpus rules.
#
# W3C extended log format field naming. Prefix decoder:
#   c-   = client side (the requesting client)
#   s-   = server side (the proxy itself)
#   cs-  = client-to-server (data the client sent)
#   sc-  = server-to-client (data the server sent back)

id: proxy
label: A web-proxy HTTP event was logged
description: Use for HTTP / HTTPS proxy log rules — outbound web traffic captured by Squid, Blue Coat, Zscaler, McAfee Web Gateway, Symantec ProxySG, Forcepoint, or any proxy that emits W3C extended-log-format records.
category_group: network
logsource:
  product: unspecified
  category: proxy
platforms:
- id: unspecified
  product: unspecified
  tier: primary
synonyms:
- HTTP proxy
- web proxy log
- Squid log
- Blue Coat
- Zscaler
- ProxySG
- web gateway
- outbound HTTP
fields:
- name: c-uri
  label: Requested URI
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  example: /oauth/idp/.well-known/openid-configuration
  note: "Full URI the client requested (path + query). Highest-signal field — most rules pivot here. Substring match catches paths regardless of query parameters."
- name: c-useragent
  label: User-Agent header
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1)
  note: "Browser / client identifier the request claimed to be. Watch for empty agents, common offensive-tool agents (curl/<version>, python-requests, sqlmap, Cobalt Strike default), and unusual / impersonating user-agents."
- name: cs-method
  label: HTTP method
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: POST
  note: "HTTP verb sent by the client. Common: GET (read), POST (data submit / often C2 beacon), PUT / DELETE (REST APIs), CONNECT (HTTPS tunnel — high-signal for non-browser tools)."
- name: cs-host
  label: Host header (destination domain)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: cs-uri
  label: Full request URI (alt schema)
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: c-uri-extension
  label: URI file extension
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
