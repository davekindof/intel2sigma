# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=msexchange-management
# Vetted-stratum rule count at last calibration: 7
# Hand-review the field selections + modifiers before ship.

id: windows_msexchange_management
label: An Exchange management cmdlet was logged
description: Use for on-premises Exchange management rules — cmdlet invocations recorded in the MSExchange Management channel, the channel that catches mailbox export abuse and ProxyShell-style post-exploitation.
category_group: os_event_log
logsource:
  product: windows
  service: msexchange-management
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- Exchange management
- msexchange-management
- Exchange cmdlet
- MailboxExportRequest
- ProxyShell
fields:
- name: Data
  label: Cmdlet invocation text
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  note: Full cmdlet line as logged, including parameters. Substring matching is the practical approach.
  example: New-MailboxExportRequest -Mailbox admin -FilePath \\localhost\c$\temp.aspx
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  note: MSExchange Management channel event; 1 and 6 carry cmdlet invocations.
  example: '1'
