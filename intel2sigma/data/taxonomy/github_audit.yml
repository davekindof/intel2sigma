# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: github/service=audit
# Vetted-stratum rule count at last calibration: 15
# Hand-review the field selections + modifiers before ship.

id: github_audit
label: A GitHub audit-log event was logged
description: Use for GitHub Audit Log rules — repo, org, secret-scanning, billing.
category_group: audit_and_identity
logsource:
  product: github
  service: audit
platforms:
- id: github
  product: github
  tier: primary
synonyms: []
fields:
- name: action
  label: Action
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
