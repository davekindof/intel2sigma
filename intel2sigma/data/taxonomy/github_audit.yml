# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: github/service=audit
# Vetted-stratum rule count at last calibration: 15
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.
#
# Single-field observation. Every SigmaHQ rule under github/audit
# pivots on `action`. Other audit-log fields (actor, repo, etc.)
# exist in the schema but no vetted rule currently uses them.

id: github_audit
label: A GitHub audit-log event was logged
description: Use for GitHub Enterprise / Cloud Audit Log rules — repo creation/deletion, org member changes, secret-scanning bypasses, OAuth app installs, branch protection changes, pages deployments, billing changes.
category_group: audit_and_identity
logsource:
  product: github
  service: audit
platforms:
- id: github
  product: github
  tier: primary
synonyms:
- GitHub audit
- GitHub enterprise audit
- repo audit
- org audit
- secret scanning bypass
- branch protection change
- OAuth app install
fields:
- name: action
  label: GitHub action name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: secret_scanning_push_protection.bypass
  note: "GitHub audit event identifier in `<scope>.<verb>` form. Common: repo.create / repo.destroy, org.add_member, secret_scanning_push_protection.bypass, repo.pages_public, oauth_application.create, members_can_create_repositories.disable."
