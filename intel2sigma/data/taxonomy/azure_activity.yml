# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=activitylogs
# Vetted-stratum rule count at last calibration: 42
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.

id: azure_activity
label: An Azure Activity Log event was logged
description: Use for Azure subscription-level activity rules — resource changes (create / delete / write), RBAC role assignments, Kubernetes / AKS operations, and any subscription-scoped Azure Resource Manager call.
category_group: audit_and_identity
logsource:
  product: azure
  service: activitylogs
platforms:
- id: azure
  product: azure
  tier: primary
synonyms:
- Azure Activity Log
- Azure ARM activity
- subscription activity
- resource change
- RBAC change
- AKS audit
fields:
- name: operationName
  label: Operation name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: MICROSOFT.SECURITY/ALERTSSUPPRESSIONRULES/WRITE
  note: "Azure Resource Manager operation in `<PROVIDER>/<RESOURCE>/<ACTION>` form, all-uppercase. Highest-signal field — most rules pivot on this. Common patterns: */WRITE (create/update), */DELETE (deletion), */ACTION (custom RPC)."
- name: properties.message
  label: Operation description
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Add service principal
  note: "Human-readable description of the operation (e.g. 'Add service principal', 'Remove service principal'). Useful for catching identity-management actions where operationName is too granular."
- name: CategoryValue
  label: Activity category
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Administrative
  note: "Top-level activity category. Common: Administrative (control-plane changes), Security, ServiceHealth, Alert, Recommendation, Policy, Autoscale."
- name: OperationNameValue
  label: Operation name (alt schema)
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: ResourceId
  label: Resource ID
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: ResourceProviderValue
  label: Resource provider
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
