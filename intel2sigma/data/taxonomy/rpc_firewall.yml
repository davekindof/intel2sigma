# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: rpc_firewall/application
# Vetted-stratum rule count at last calibration: 17
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.

id: rpc_firewall
label: An RPC Firewall event was logged
description: Use for RPC Firewall (zeronetworks/rpcfirewall) rules — protocol-filter alerts on Windows MS-RPC traffic, typically catching lateral-movement attempts via DCOM, scheduled-task RPC, or service-control RPC.
category_group: network
logsource:
  product: rpc_firewall
  category: application
platforms:
- id: rpc_firewall
  product: rpc_firewall
  tier: primary
synonyms:
- RPC Firewall
- rpcfirewall
- MS-RPC filter
- DCOM filter
- lateral movement RPC
- RPC interception
fields:
- name: InterfaceUuid
  label: RPC interface UUID
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: 367abb81-9844-35f1-ad32-98f038001003
  note: "GUID identifying the MS-RPC interface targeted. High-signal field — almost every rule pivots here. Common: 367abb81-… = SCM (services), 86d35949-… = ATSvc (scheduled tasks), 4b324fc8-… = SrvSvc, 12345778-… = LSARPC."
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  example: "3"
  note: "RPC Firewall event code. 3 = RPC call detected (the audit event most rules use); 4/5 = blocked / allowed states."
- name: OpNum
  label: RPC operation number
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: "12"
  note: "Numeric operation index within the RPC interface. Pair with InterfaceUuid to filter to a specific function call (e.g. SCM OpNum 12 = CreateServiceW)."
- name: EventLog
  label: Event log channel
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
