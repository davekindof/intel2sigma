# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=signinlogs
# Vetted-stratum rule count at last calibration: 24
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.

id: azure_signin
label: An Azure AD / Entra ID sign-in event was logged
description: Use for Azure AD / Entra ID sign-in log rules — interactive logons, MFA challenges, conditional-access evaluation, device-code flow, ROPC, password-spray detection.
category_group: audit_and_identity
logsource:
  product: azure
  service: signinlogs
platforms:
- id: azure
  product: azure
  tier: primary
synonyms:
- Azure AD sign-in
- Entra ID sign-in
- AAD logon
- conditional access
- MFA challenge
- password spray
- device code flow
fields:
- name: ResultType
  label: Sign-in result code
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: "50074"
  note: "Numeric Azure AD sign-in result code (string, despite looking numeric). High-signal field. Common: 0 (success), 50053 (account locked), 50074 (strong-auth required), 500121 (MFA failed)."
- name: Status
  label: Sign-in status
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: failure
  note: "High-level outcome: 'Success', 'failure', or specific MFA states like 'MFA Denied'. Filter Status='failure' as the overlay for password-spray / brute-force rules."
- name: ResultDescription
  label: Result description
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: User account is disabled. The account has been disabled by an administrator.
  note: "Free-text explanation of the result. Useful when ResultType is too granular and you want to match a class of failures (e.g. 'Authentication failed during strong authentication request')."
- name: AuthenticationRequirement
  label: Authentication requirement
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: properties.message
  label: Sign-in properties message
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: Location
  label: Sign-in country/location
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: Count
  label: Sign-in attempt count
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userAgent
  label: User agent
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
