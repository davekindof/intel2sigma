# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=application
# Vetted-stratum rule count at last calibration: 23
# Hand-reviewed for verbiage 2026-04-26 (F1d).

id: application_log
label: A Windows Application event was logged
description: Use for Windows Application event channel rules — third-party app errors, .NET runtime crashes, app installer events, Windows Error Reporting.
category_group: os_event_log
logsource:
  product: windows
  service: application
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- Windows Application log
- Application event log
- WER report
- Application crash
- .NET runtime error
fields:
- name: Provider_Name
  label: Provider name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: Microsoft-Windows-RestartManager
  note: "The component that logged the event. Common providers include Application Error, Microsoft-Windows-RestartManager, MsiInstaller, .NET Runtime."
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  example: "1000"
  note: "Application-channel event ID. 1000 = application error/crash, 1001 = Windows Error Reporting, 1002 = application hang, 11707/11724 = MSI install/uninstall."
- name: Data
  label: Event data
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  example: '\Users\Public\evil.exe'
  note: "Free-form payload from the event — often the path of the crashing or installed binary. Substring match with 'contains' is the typical idiom."
- name: Level
  label: Severity level
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: AppName
  label: Application name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: Message
  label: Event message
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
