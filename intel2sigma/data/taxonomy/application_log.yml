# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=application
# Vetted-stratum rule count at last calibration: 23
# Hand-review the field selections + modifiers before ship.

id: application_log
label: A Windows Application event was logged
description: Use for Windows Application event channel rules (third-party app + .NET runtime errors).
category_group: os_event_log
logsource:
  product: windows
  service: application
platforms:
  - id: windows
    product: windows
    tier: primary
synonyms: []
fields:
  - name: Provider_Name
    label: Provider_Name
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: EventID
    label: EventID
    type: int
    default_modifier: exact
    allowed_modifiers:
      - exact
      - gt
      - gte
      - lt
      - lte
  - name: Data
    label: Data
    type: string
    default_modifier: contains
    allowed_modifiers:
      - contains
      - startswith
      - endswith
      - exact
      - re
  - name: Level
    label: Level
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: AppName
    label: AppName
    type: string
    default_modifier: exact
    allowed_modifiers:
      - exact
      - contains
      - startswith
      - endswith
      - re
  - name: Message
    label: Message
    type: string
    default_modifier: contains
    allowed_modifiers:
      - contains
      - startswith
      - endswith
      - exact
      - re
