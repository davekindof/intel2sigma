# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=firewall-as
# Vetted-stratum rule count at last calibration: 8
# Hand-review the field selections + modifiers before ship.

id: windows_firewall_as
label: A Windows Firewall rule change was logged
description: Use for Windows Defender Firewall rules — rule additions, deletions and profile changes, which attackers make to open a listener or punch a hole for lateral movement.
category_group: network
logsource:
  product: windows
  service: firewall-as
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- Windows Firewall
- firewall-as
- advanced security
- firewall rule change
- netsh advfirewall
fields:
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  note: Microsoft-Windows-Windows Firewall With Advanced Security event. 2004 adds a rule, 2006 deletes one.
  example: '2004'
- name: ModifyingApplication
  label: Modifying application
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Process that changed the firewall configuration. netsh.exe and PowerShell are the usual abuse route.
  example: C:\Windows\System32\netsh.exe
- name: Action
  label: Rule action
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: What the rule does with matching traffic — allow or block.
  example: Allow
- name: ApplicationPath
  label: Rule application path
  type: string
  default_modifier: endswith
  allowed_modifiers:
  - endswith
  - contains
  - startswith
  - exact
  - re
  note: Executable the firewall rule grants or denies traffic for.
  example: C:\Users\Public\evil.exe
