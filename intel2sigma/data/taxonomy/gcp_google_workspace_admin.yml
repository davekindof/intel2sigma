# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: gcp/service=google_workspace.admin
# Vetted-stratum rule count at last calibration: 7
# Hand-review the field selections + modifiers before ship.

id: gcp_google_workspace_admin
label: A Google Workspace admin action was logged
description: Use for Google Workspace Admin audit rules — tenant-level administrative changes such as role grants, security-setting edits and application access changes.
category_group: audit_and_identity
logsource:
  product: gcp
  service: google_workspace.admin
platforms:
- id: gcp
  product: gcp
  tier: primary
synonyms:
- Google Workspace
- GSuite admin
- Workspace audit
- admin console
- tenant admin change
fields:
- name: eventName
  label: Admin event name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: The specific admin action recorded by Workspace, in its snake_case vocabulary.
  example: ADD_PRIVILEGE
- name: eventService
  label: Reporting service
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Which Workspace service emitted the event.
  example: admin.googleapis.com
- name: new_value
  label: New setting value
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Value the setting was changed to — the half that says whether the change weakened security.
  example: 'true'
- name: setting_name
  label: Setting name
  type: string
  default_modifier: startswith
  allowed_modifiers:
  - startswith
  - contains
  - endswith
  - exact
  - re
  note: Which configuration setting the admin altered.
