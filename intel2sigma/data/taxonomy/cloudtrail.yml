# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: aws/service=cloudtrail
# Vetted-stratum rule count at last calibration: 55
# Hand-review the field selections + modifiers before ship.

id: cloudtrail
label: An AWS CloudTrail event was logged
description: Use for AWS CloudTrail rules — IAM, S3, EC2, etc.
category_group: audit_and_identity
logsource:
  product: aws
  service: cloudtrail
platforms:
- id: aws
  product: aws
  tier: primary
synonyms: []
fields:
- name: eventName
  label: Event name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: eventSource
  label: Event source
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userAgent
  label: User agent
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: errorCode
  label: Error code
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userIdentity.type
  label: User identity.type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userIdentity.arn
  label: User identity.arn
  type: string
  default_modifier: re
  allowed_modifiers:
  - re
  - contains
  - startswith
  - endswith
  - exact
