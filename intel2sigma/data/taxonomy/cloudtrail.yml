# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: aws/service=cloudtrail
# Vetted-stratum rule count at last calibration: 55
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules at intel2sigma/data/sigmahq_corpus.json.

id: cloudtrail
label: An AWS CloudTrail event was logged
description: Use for AWS CloudTrail rules — IAM, S3, EC2, Lambda, KMS, GuardDuty and any other AWS service that writes to CloudTrail.
category_group: audit_and_identity
logsource:
  product: aws
  service: cloudtrail
platforms:
- id: aws
  product: aws
  tier: primary
synonyms:
- AWS CloudTrail
- AWS audit log
- CloudTrail event
- IAM activity
- S3 access log
- AWS API call
- aws audit
fields:
- name: eventName
  label: API event name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: CreateFunctionUrlConfig
  note: "The AWS API call name (PascalCase). High-signal field — most CloudTrail rules pivot on this. Pair with eventSource to disambiguate cross-service collisions."
- name: eventSource
  label: AWS service source
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: kms.amazonaws.com
  note: "The AWS service that emitted the event, in `<service>.amazonaws.com` form (e.g. `iam.amazonaws.com`, `ecs.amazonaws.com`, `s3.amazonaws.com`). Always paired with eventName to scope the API."
- name: userAgent
  label: API client user agent
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  example: TruffleHog
  note: "Identifies the client SDK or tool that made the API call. Watch for known offensive-tool agents (TruffleHog, Pacu, ScoutSuite) and unusual scripted clients."
- name: errorCode
  label: Error code
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userIdentity.type
  label: User identity type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
- name: userIdentity.arn
  label: User identity ARN
  type: string
  default_modifier: re
  allowed_modifiers:
  - re
  - contains
  - startswith
  - endswith
  - exact
