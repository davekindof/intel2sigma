# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: linux/unspecified
# Vetted-stratum rule count at last calibration: 15
# Hand-review the field selections + modifiers before ship.

id: linux_misc
label: A Linux event without a specific category
description: Use for Linux logsources that don't fit a more specific category.
category_group: os_event_log
logsource:
  product: linux
  category: unspecified
platforms:
  - id: linux
    product: linux
    tier: primary
synonyms: []
fields:
  - name: EventID
    label: Event ID
    type: int
    default_modifier: exact
    allowed_modifiers:
      - exact
