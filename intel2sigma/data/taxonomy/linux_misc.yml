# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: linux/unspecified
# Vetted-stratum rule count at last calibration: 15
# Hand-reviewed for verbiage 2026-04-26 (F1d).
#
# Linux rules without a specific category. Heterogeneous bucket — the
# only universally-present field across the corpus rules in this slot
# is EventID, so that's the one we surface. Authors building rules
# here typically have something specific in mind and will reach for
# the freeform-observation entry on Stage 0 if their need isn't met.

id: linux_misc
label: A Linux log event was logged
description: Use for Linux logsources that don't fit a more specific category — generic syslog, application-specific logs, or rules where the source is intentionally broad.
category_group: os_event_log
logsource:
  product: linux
  category: unspecified
platforms:
  - id: linux
    product: linux
    tier: primary
synonyms:
  - generic linux log
  - syslog
  - linux unspecified
  - linux event
fields:
  - name: EventID
    label: Event ID
    type: int
    default_modifier: exact
    allowed_modifiers:
      - exact
    note: "Numeric event identifier for log-source-specific events. Most linux/unspecified rules in the corpus use this with a SIEM-side mapping; check your pipeline's documentation for available IDs."
