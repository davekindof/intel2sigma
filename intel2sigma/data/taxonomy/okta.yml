# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: okta/service=okta
# Vetted-stratum rule count at last calibration: 21
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.

id: okta
label: An Okta system event was logged
description: Use for Okta System Log rules — authentication attempts, MFA challenges, app sign-on policies, user lifecycle, API token creation, ThreatInsight detections.
category_group: audit_and_identity
logsource:
  product: okta
  service: okta
platforms:
- id: okta
  product: okta
  tier: primary
synonyms:
- Okta system log
- Okta auth
- Okta MFA
- Okta API token
- ThreatInsight
- Okta sign-in
fields:
- name: eventtype
  label: Okta event type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: system.api_token.create
  note: "Dotted lowercase event identifier — Okta's primary classifier. High-signal: most rules pivot here. Common: user.session.start, security.threat.detected, system.api_token.create, user.account.privilege.grant, application.user_membership.add."
- name: displaymessage
  label: Display message
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: User attempted unauthorized access to app
  note: "Human-readable description shown in the Okta admin console. Useful for catching states the eventtype field can't (e.g. 'Max sign in attempts exceeded', 'FastPass declined phishing attempt')."
- name: debugContext.debugData.requestUri
  label: Request URI (debug)
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  example: /api/v1/users
  note: "Okta API path the request targeted. Useful for filtering admin API calls (paths under /api/v1/users, /api/v1/apps, /api/v1/policies)."
- name: actor.alternateid
  label: Actor identifier (email or app id)
  type: string
  default_modifier: re
  allowed_modifiers:
  - re
  - contains
  - startswith
  - endswith
  - exact
- name: debugcontext.debugdata.behaviors
  label: ThreatInsight behaviors
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
- name: debugcontext.debugdata.logonlysecuritydata
  label: Log-only security data
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
