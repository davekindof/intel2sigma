# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: fortigate/service=event
# Vetted-stratum rule count at last calibration: 7
# Hand-review the field selections + modifiers before ship.

id: fortigate_event
label: A FortiGate event was logged
description: Use for FortiGate appliance rules — administrative actions, configuration changes and VPN events on the firewall itself.
category_group: network
logsource:
  product: fortigate
  service: event
platforms:
- id: fortigate
  product: fortigate
  tier: primary
synonyms:
- FortiGate
- Fortinet
- firewall event
- appliance admin
- VPN event
fields:
- name: action
  label: Event action
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: The action FortiOS recorded, such as a login, a configuration edit or a tunnel operation.
  example: login
- name: cfgpath
  label: Configuration path
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Which part of the device configuration was touched — the useful discriminator for change-detection rules.
  example: system.admin
