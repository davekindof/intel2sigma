# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=riskdetection
# Vetted-stratum rule count at last calibration: 19
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.
#
# This is a single-field observation type — the entire SigmaHQ rule
# catalog under riskdetection pivots on ``riskEventType``. Other
# fields (riskLevel, riskState, etc.) exist in the schema but no
# rule in the vetted stratum currently uses them. Skipping them
# keeps the dropdown focused on what actually fires.

id: azure_risk
label: An Azure AD / Entra ID identity-protection risk was detected
description: Use for Azure AD / Entra ID Identity Protection risk-event rules — Microsoft's risk engine flagging suspicious sign-ins, leaked credentials, anomalous travel, and password-spray attempts.
category_group: audit_and_identity
logsource:
  product: azure
  service: riskdetection
platforms:
- id: azure
  product: azure
  tier: primary
synonyms:
- Identity Protection risk
- AAD risk detection
- Entra ID risk
- leaked credentials
- impossible travel
- suspicious sign-in
fields:
- name: riskEventType
  label: Risk event type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: passwordSpray
  note: "Microsoft's risk-detection classifier output. Common values: passwordSpray, suspiciousIPAddress, maliciousIPAddress, leakedCredentials, anomalousToken, unfamiliarFeatures, atypicalTravel, anonymousIPAddress."
