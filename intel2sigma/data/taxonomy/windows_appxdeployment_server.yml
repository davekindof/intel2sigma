# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: windows/service=appxdeployment-server
# Vetted-stratum rule count at last calibration: 9
# Hand-review the field selections + modifiers before ship.

id: windows_appxdeployment_server
label: An AppX package deployment was logged
description: Use for Windows AppX/MSIX package deployment rules — sideloaded or malicious app packages installed via the AppXDeployment-Server operational channel.
category_group: os_event_log
logsource:
  product: windows
  service: appxdeployment-server
platforms:
- id: windows
  product: windows
  tier: primary
synonyms:
- AppX deployment
- MSIX install
- sideloaded app
- appxdeployment-server
- package install
fields:
- name: EventID
  label: Event ID
  type: int
  default_modifier: exact
  allowed_modifiers:
  - exact
  - gt
  - gte
  - lt
  - lte
  note: AppXDeployment-Server operational event. 854 covers deployment requests including sideloading.
  example: '854'
- name: Path
  label: Package source path
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  note: Filesystem or URL path the package was deployed from. Remote or user-writable paths are the interesting case.
  example: C:\Users\Public\evil.appx
- name: PackageFullName
  label: Package full name
  type: string
  default_modifier: contains
  allowed_modifiers:
  - contains
  - startswith
  - endswith
  - exact
  - re
  note: 'Full AppX identity: name, version, architecture and publisher hash.'
  example: EvilApp_1.0.0.0_x64__8wekyb3d8bbwe
- name: CallingProcess
  label: Calling process
  type: string
  default_modifier: startswith
  allowed_modifiers:
  - startswith
  - contains
  - endswith
  - exact
  - re
  note: Process that requested the deployment. Anything other than the usual installer stack is worth a look.
  example: C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
- name: ErrorCode
  label: Deployment error code
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Non-zero when deployment failed; failed sideload attempts are themselves a signal.
- name: Flags
  label: Deployment flags
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Bitmask of deployment options requested by the caller.
- name: HasFullTrust
  label: Requests full trust
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Whether the package asked for full-trust privileges.
- name: PackageSourceUri
  label: Package source URI
  type: string
  default_modifier: startswith
  allowed_modifiers:
  - startswith
  - contains
  - endswith
  - exact
  - re
  note: Origin URI when the package came from a remote location.
