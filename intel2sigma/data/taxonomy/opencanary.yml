# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: opencanary/application
# Vetted-stratum rule count at last calibration: 24
# Hand-reviewed for verbiage 2026-04-26 (F1d-β).
# Examples mined from real corpus rules.
#
# Single-field observation type. OpenCanary rules in the SigmaHQ
# corpus all pivot on the numeric ``logtype`` (an OpenCanary
# constant indicating which honeypot service tripped). Other event
# fields are emitted but not currently used by any vetted rule.

id: opencanary
label: An OpenCanary honeypot fired
description: Use for OpenCanary deception-tool alert rules — honeypot services for FTP, SSH, Telnet, HTTP, MSSQL, Redis, Git, MySQL, SMB, NTP, RDP, SNMP, and TFTP that fire on any interaction.
category_group: audit_and_identity
logsource:
  product: opencanary
  category: application
platforms:
- id: opencanary
  product: opencanary
  tier: primary
synonyms:
- OpenCanary
- honeypot
- canary
- deception
- decoy
- canarytoken
fields:
- name: logtype
  label: Honeypot service code
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  example: "5000"
  note: "Numeric code (string) identifying which OpenCanary honeypot service fired. Common: 2000=FTP, 3000=HTTP/HTTPS, 4000=SSH, 5000=SMB, 6000=Telnet, 7000=MSSQL, 8001=Redis, 9000=Git, 10000=MySQL. Any hit is high-signal — production traffic should never reach a canary."
