# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: zeek/service=dns
# Vetted-stratum rule count at last calibration: 5
# Hand-review the field selections + modifiers before ship.

id: zeek_dns
label: A DNS transaction was observed by Zeek
description: Use for Zeek dns.log rules — network-sensor DNS visibility, which sees resolution for every host on the segment rather than only instrumented endpoints.
category_group: network
logsource:
  product: zeek
  service: dns
platforms:
- id: zeek
  product: zeek
  tier: primary
synonyms:
- Zeek DNS
- Bro DNS
- dns.log
- network DNS
- sensor DNS
fields:
- name: query
  label: Queried domain name
  type: string
  default_modifier: endswith
  allowed_modifiers:
  - endswith
  - contains
  - startswith
  - exact
  - re
  note: Domain from the DNS request as the sensor saw it on the wire.
  example: anonfiles.com
- name: answers
  label: Answer set
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Addresses or names returned in the response.
  example: 203.0.113.10
- name: Z
  label: Reserved Z flag
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Reserved header bit. Non-zero values are anomalous and appear in tunnelling traffic.
  example: '1'
- name: id.resp_p
  label: Responder port
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Destination port of the transaction — non-53 DNS is itself suspicious.
  example: '53'
- name: qtype_name
  label: Query type name
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Record type as text. TXT and NULL carry tunnelling and exfiltration.
  example: TXT
- name: rejected
  label: Rejected flag
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Whether the resolver refused the query.
