# Auto-generated by scripts/generate_taxonomy_yaml.py
# Source key: azure/service=pim
# Vetted-stratum rule count at last calibration: 7
# Hand-review the field selections + modifiers before ship.

id: azure_pim
label: An Entra Privileged Identity Management event was logged
description: Use for Microsoft Entra PIM rules — privileged role activations, elevation requests and PIM risk detections, where an attacker who already holds a foothold escalates to an admin role.
category_group: audit_and_identity
logsource:
  product: azure
  service: pim
platforms:
- id: azure
  product: azure
  tier: primary
synonyms:
- PIM
- Privileged Identity Management
- Entra PIM
- role activation
- privileged elevation
- Azure AD PIM
fields:
- name: riskEventType
  label: Risk event type
  type: string
  default_modifier: exact
  allowed_modifiers:
  - exact
  - contains
  - startswith
  - endswith
  - re
  note: Entra risk-detection classifier attached to the PIM event, naming the suspicious behaviour.
  example: unfamiliarFeatures
